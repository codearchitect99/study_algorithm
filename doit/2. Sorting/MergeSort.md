## MergeSort

분할 정복 방식을 사용해 데이터를 분할하고 분할한 집단을 정렬하며 합치는 알고리즘

시간복잡도: O(nlongn)

1. 배열을 반으로 계속 분할한다.
2. 원소가 1개가 될 때까지 divide를 반복한다.
3. 분할된 작은 배열들을 정렬하며 merge 한다.
4. 병합 결과를 임시 배열에 저장한다.
5. 모든 병합이 끝나면 전체가 정렬된다.